# HunchSense Website

Static GitHub Pages site for `hunchsense.com`.

The checked-in page is a compiled static artifact. The contact form integration
is kept in `contact-form.js` and `contact-form.css`; its AWS infrastructure and
runbook are in [`infra/contact-form`](infra/contact-form/README.md).
